duaRouter

The duaRouter generates routes based on the shortest path. To do this, a batch file ("duarouter.bat") must be executed.
The routes are saved in an output file.

A separate output file can then be created to compare the counting data with the output data.